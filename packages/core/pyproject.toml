[project]
name = "pl-agent-core"
version = "0.1.0"
description = "幻兽帕鲁配种 Agent — 核心引擎"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pl_agent"]

[tool.pytest.ini_options]
testpaths = ["pl_agent/core/__tests__"]
